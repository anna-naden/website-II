Fix open(,'wt') writing county markers - it was not being parsed correctly by the javascript.
Write web content to /var/www/html and use "aws s3 sync" to sync the content between that local folder and the s3 bucket.

